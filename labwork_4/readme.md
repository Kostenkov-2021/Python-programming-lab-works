# Программирование Python

# Лабораторная работа №4

## Костенков Данил Денисович

## Группа P4150


# Сравнение производительности рекурсивной и итеративной реализации факториала


Проект для сравнения времени выполнения рекурсивной и итеративной реализаций вычисления факториала с визуализацией результатов.

## 📋 Оглавление

- [Обзор](#обзор)
- [Установка и запуск](#установка-и-запуск)
- [Структура проекта](#структура-проекта)
- [Использование](#использование)
- [Результаты и выводы](#результаты-и-выводы)

## Обзор

Этот проект демонстрирует разницу в производительности между двумя подходами к вычислению факториала:
- **Рекурсивная реализация** - использует вызовы самой себя
- **Итеративная реализация** - использует циклы

Проект включает:
- Чистые реализации обоих методов с аннотациями типов
- Точные замеры времени с использованием `timeit`
- Визуализацию результатов с помощью `matplotlib`
- Детальную документацию в соответствии с PEP-257

## 🚀 Установка и запуск

### Требования
- Python 3.8 или выше
- Установленные зависимости:

```bash
pip install matplotlib
```

### Запуск проекта

1. Клонируйте репозиторий:
```bash
git clone https://github.com/Kostenkov-2021/Python-programming-lab-works/labwork_4.git
cd labwork_4
```

2. Запустите основной скрипт:
```bash
python main.py
```

3. Для запуска чистого бенчмарка на одном числе:
```bash
python single_benchmark.py
```

## Структура проекта

│
├── factorial.py          # Реализации функций факториала
├── benchmark.py          # Логика замера времени
├── main.py              # Основной скрипт для построения графиков
├── single_benchmark.py  # Чистый бенчмарк для одного числа
└── README.md           # Документация
```

## Использование

### Основные функции

#### `factorial.py`
```python
def fact_recursive(n: int) -> int:
    """Рекурсивное вычисление факториала."""
    
def fact_iterative(n: int) -> int:
    """Итеративное вычисление факториала."""
```

#### `benchmark.py`
```python
def measure_time(func: Callable[[int], int], numbers: List[int]) -> List[float]:
    """Замеряет среднее время выполнения функции."""
```

### Пример использования

```python
from factorial import fact_recursive, fact_iterative

# Вычисление факториала
print(f"Рекурсивно: 5! = {fact_recursive(5)}")  # 120
print(f"Итеративно: 5! = {fact_iterative(5)}")  # 120
```

## Результаты и выводы

### Методология тестирования
- Тестирование проводилось для чисел от 0 до 20
- Каждое измерение повторялось 5 раз по 100,000 итераций
- Использовался минимальный результат для исключения выбросов

### Ключевые наблюдения

1. **Производительность**:
   - Итеративный метод работает значительно быстрее на больших числах
   - Рекурсивный метод имеет накладные расходы на вызовы функций

2. **Ограничения**:
   - Рекурсивная версия ограничена глубиной стека (обычно ~1000 вызовов)
   - Итеративная версия не имеет таких ограничений

3. **Память**:
   - Рекурсивный подход потребляет больше памяти из-за стека вызовов
   - Итеративный подход более эффективен по памяти

### Пример результатов
```
Рекурсивная версия для n=20: 0.045673 сек
Итеративная версия для n=20: 0.023451 сек
```


---

**Примечание**: Для получения точных результатов рекомендуется запускать тесты на изолированной системе без фоновых процессов, влияющих на производительность.

